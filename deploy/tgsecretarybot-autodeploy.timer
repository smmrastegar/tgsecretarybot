[Unit]
Description=Run tgsecretarybot auto-deploy every minute

[Timer]
# 60s after boot, then every minute. Persistent catches up a missed
# tick after downtime. The service itself no-ops when HEAD is unchanged.
OnBootSec=60
OnUnitActiveSec=60
Persistent=true

[Install]
WantedBy=timers.target
